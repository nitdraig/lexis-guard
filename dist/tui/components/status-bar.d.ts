import React from 'react';
interface StatusBarProps {
    target: string;
    mode: string;
    durationMs: number;
    incomplete: boolean;
    requests?: number;
}
export declare function StatusBar({ target, mode, durationMs, incomplete, requests }: StatusBarProps): React.ReactElement;
export {};
//# sourceMappingURL=status-bar.d.ts.map