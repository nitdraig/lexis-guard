/**
 * Deterministic hash of a finding's identity fields.
 * Used to match findings against `.lexisignore` entries.
 */
export declare function generateFindingHash(ruleId: string, path: string, method: string): string;
//# sourceMappingURL=finding-hash.d.ts.map